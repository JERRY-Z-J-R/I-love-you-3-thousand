<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.jerry.mapper.OrderMapper">
    <!-- 生成订单（单个商品） -->
    <insert id="addG">
        INSERT INTO 'order'(userid, ordertime, orderpay, orderstate)
        VALUES (#{userid}, #{ordertime}, #{orderpay}, #{orderstate})
    </insert>

    <!-- 生成订单（购物车） -->
    <insert id="addC">
        INSERT INTO 'order'(userid, ordertime, orderpay, orderstate)
        VALUES (#{userid}, #{ordertime}, #{orderpay}, #{orderstate})
    </insert>
</mapper>